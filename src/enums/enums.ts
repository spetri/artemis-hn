export enum Position {
  LEFT = 'Left',
  RIGHT = 'Right'
}

export enum ThreadReplies {
  AUTO = 'auto',
  NONE = 'none',
  ALL = 'all'
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
  AUTO = 'auto'
}

export enum HackerNews {
  HOME = 'home',
  BEST = 'best',
  TOP = 'top',
  NEW = 'new',
  SHOW = 'show',
  ASK = 'ask',
  JOB = 'job'
}

export enum StoryDatatype {
  STORY = 'story',
  COMMENT = 'comment',
  POLL = 'poll',
  JOB = 'job'
}
